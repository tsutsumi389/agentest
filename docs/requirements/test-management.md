# 機能一覧

## 概要

テスト管理ツール「Agentest」の機能要件。Coding Agent との連携を前提とした設計。

---

## MVP 機能（初期リリース）

### 1. プロジェクト管理

| ID | 機能 | 説明 |
|----|------|------|
| PRJ-001 | プロジェクト作成 | ユーザーまたは組織に所属するプロジェクトを作成 |
| PRJ-002 | プロジェクト一覧 | 所属するプロジェクトの一覧表示 |
| PRJ-003 | プロジェクト設定 | 名前、説明、メンバー管理 |
| PRJ-004 | プロジェクト履歴 | 変更履歴を保持し、任意のバージョンに復元可能 |
| PRJ-005 | プロジェクト削除 | プロジェクトを削除（論理削除） |
| PRJ-006 | プロジェクト検索 | プロジェクト名・説明で検索 |
| PRJ-007 | プロジェクト環境設定 | 各環境（dev/stg/prod等）のエンドポイントURL を複数登録（別テーブル） |

### 2. テストスイート管理

| ID | 機能 | 説明 |
|----|------|------|
| TS-001 | テストスイート作成 | プロジェクト内にテストスイートを作成 |
| TS-002 | テストスイート前提条件 | スイート単位の前提条件を複数登録（別テーブル） |
| TS-003 | テストスイート一覧 | プロジェクト内のスイート一覧表示 |
| TS-004 | テストスイート履歴 | 変更履歴を保持し、任意のバージョンに復元可能 |
| TS-005 | テストスイートレビュー | レビューコメントの登録・返信 |
| TS-006 | テストスイート削除 | テストスイートを削除（論理削除） |
| TS-007 | テストスイート検索 | スイート名・前提条件で検索 |
| TS-008 | テストスイートフィルタ | ステータス・作成者・更新日でフィルタ |

### 3. テストケース管理

| ID | 機能 | 説明 |
|----|------|------|
| TC-001 | テストケース作成 | テストスイート内にテストケースを作成 |
| TC-002 | テストケース構造 | 以下を別テーブルで管理：<br>- 前提条件（複数）<br>- 手順（複数）<br>- 期待値（複数） |
| TC-003 | テストケースコピー | 同一プロジェクト内のテストケースをコピー |
| TC-004 | テストケース参照入力 | `@テストスイート/テストケース` 形式で参照・コピー候補を表示 |
| TC-005 | テストケース履歴 | 変更履歴を保持し、任意のバージョンに復元可能 |
| TC-006 | テストケースレビュー | レビューコメントの登録・返信 |
| TC-007 | テストケース削除 | テストケースを削除（論理削除） |
| TC-008 | テストケース検索 | タイトル・手順・期待値で全文検索 |
| TC-009 | テストケースフィルタ | ステータス・作成者・更新日でフィルタ |
| TC-010 | テストケースソート | 作成日・更新日・タイトルでソート |

### 4. テスト実行

| ID | 機能 | 説明 |
|----|------|------|
| EX-001 | テスト実行開始 | テストスイート単位でテストを実行（対象環境を選択） |
| EX-002 | スナップショット作成 | 実行開始時にテストスイート・テストケースをスナップショット |
| EX-003 | 繰り返し実行 | 同じテストスイートを何度でも実行可能 |
| EX-004 | 手順実施記録 | 各前提条件・手順の実施状況を記録 |
| EX-005 | 期待値判定記録 | 各期待値の合否を記録 |
| EX-006 | 実行結果一覧 | 過去の実行結果を一覧表示 |
| EX-007 | エビデンス添付 | 実行結果にファイル（スクリーンショット・ログ等）を添付 |

### 5. Coding Agent 連携

| ID | 機能 | 説明 |
|----|------|------|
| AG-001 | Agent テストスイート作成 | Coding Agent がテストスイートを作成 |
| AG-002 | Agent テストスイート編集 | Coding Agent がテストスイートを編集 |
| AG-003 | Agent テストケース作成 | Coding Agent がテストケースを作成 |
| AG-004 | Agent テストケース編集 | Coding Agent がテストケースを編集 |
| AG-005 | Agent 作成レビュー | Agent が作成・編集した内容をレビュー・承認 |
| AG-006 | Agent テスト実行 | Coding Agent がテストを実行 |
| AG-007 | 実施記録 | Agent が前提条件・手順を確実に実施したか記録 |
| AG-008 | リアルタイム反映 | Agent のテスト実施状況をリアルタイムで Web 画面に反映 |
| AG-009 | MCP サーバー連携 | Streamable HTTP で MCP サーバーと連携 |
| AG-010 | Agent 識別 | MCP リクエストヘッダーで Agent 操作を識別・記録 |
| AG-011 | Agent セッション管理 | Agent のハートビート監視と自動セッション終了 |

### 6. レビューコメント

| ID | 機能 | 説明 |
|----|------|------|
| RC-001 | コメント登録 | テストスイート・テストケースにレビューコメントを登録 |
| RC-002 | コメント返信 | レビューコメントに対する返信（スレッド形式） |
| RC-003 | Agent コメント確認 | Coding Agent のレビューコメントを確認 |
| RC-004 | コメント対応修正 | レビューコメントに対応した修正を実施 |
| RC-005 | コメント解決 | 対応完了したコメントを解決済みにする |

### 7. 同時編集制御

| ID | 機能 | 説明 |
|----|------|------|
| CE-001 | 編集ロック基盤 | テストスイート・テストケース編集時に他ユーザーをブロック |
| CE-002 | 編集中表示 | 編集中のユーザー名をリアルタイム表示 |
| CE-003 | 人-人同時編集 | 先着順ロック、有効期限 90 秒 |
| CE-004 | Agent-人同時編集 | **楽観的ロック**: Agent は人間のロックを尊重し、ロック中は更新拒否 |
| CE-005 | 自動ロック解除 | ハートビート途絶 60 秒で自動解除（人間ユーザーのみ） |
| CE-006 | 強制ロック解除 | 管理者による強制解除 |

### 8. 認証・認可

| ID | 機能 | 説明 |
|----|------|------|
| AU-001 | OAuth ログイン | GitHub / Google アカウントでログイン |
| AU-002 | 組織メンバー管理 | 組織へのメンバー招待・権限設定 |
| AU-003 | プロジェクト権限 | プロジェクト単位のアクセス制御 |

---

## Phase 2 機能（将来）

| ID | 機能 | 説明 |
|----|------|------|
| P2-001 | テストケースインポート | CSV/Excel からテストケースをインポート |
| P2-002 | テストケースエクスポート | CSV/Excel へエクスポート |
| P2-003 | テストレポート | 実行結果のレポート生成 |
| P2-004 | Slack 通知 | テスト完了時の Slack 通知 |
| P2-005 | API 公開 | 外部システム連携用 API |
| P2-006 | テストケーステンプレート | よく使うテストケースのテンプレート化 |

---

## 非機能要件

### パフォーマンス

| 項目 | 要件 |
|------|------|
| API レスポンス | 95 パーセンタイルで 500ms 以内 |
| リアルタイム反映 | Agent 操作から 1 秒以内に Web 画面に反映 |
| 検索レスポンス | 全文検索は 2 秒以内 |
| 同時接続 | WebSocket 1,000 接続/インスタンス |

### データ保持

| 項目 | 要件 |
|------|------|
| 履歴保持期間 | 無期限（ストレージ容量による制限） |
| 履歴件数上限 | エンティティあたり 1,000 件（超過時は古いものを削除） |
| スナップショット保持 | 実行結果と同期間保持 |
| 論理削除データ | 30 日後に物理削除 |

### 可用性

| 項目 | 要件 |
|------|------|
| 稼働率 | 99.9%（月間ダウンタイム 43 分以内） |
| バックアップ | 日次フルバックアップ、1 時間ごとの差分 |
| リカバリ | RPO 1 時間、RTO 4 時間 |

### Agent セッション

| 項目 | 要件 |
|------|------|
| ハートビート間隔 | 30 秒 |
| ロック有効期限 | ハートビート途絶から 60 秒後に自動解除 |
| セッションタイムアウト | 無操作 30 分で自動終了 |

---

## 機能詳細

### TC-004: テストケース参照入力

**動作:**
1. テストケースのタイトル入力欄で `@` を入力
2. テストスイート名の候補をリスト表示
3. スイート選択後 `/` を入力
4. テストケース名の候補をリスト表示
5. 選択するとそのテストケースの内容をコピー

**入力例:**
```
@ログイン機能/正常系ログイン
```

### PRJ-007: プロジェクト環境設定

**目的:**
Coding Agent がテスト実行時にどの環境（エンドポイント）にアクセスすればよいかを明確にする。

**環境設定構造:**
```
ProjectEnvironment
├── id
├── projectId
├── name (例: "開発環境", "ステージング", "本番")
├── slug (例: "dev", "stg", "prod")
├── baseUrl (例: "https://dev.example.com")
├── description (任意)
├── isDefault (デフォルト環境フラグ)
├── sortOrder (表示順)
├── createdAt
└── updatedAt
```

**機能:**
- プロジェクトごとに複数の環境を登録可能
- 各環境にエンドポイント URL を設定
- テスト実行時に対象環境を選択
- Agent はテスト実行コンテキストから対象環境の URL を取得

**使用例:**
```
プロジェクト: ECサイト
├── dev:  https://dev.ec-site.example.com
├── stg:  https://stg.ec-site.example.com
└── prod: https://ec-site.example.com
```

---

### 履歴管理（PRJ-004, TS-004, TC-005）

**対象:**
- プロジェクト
- テストスイート
- テストケース

**記録対象:**
- 変更日時
- 変更者（人 or Agent）
- 変更内容（差分）
- 変更理由（任意）

**機能:**
- 履歴一覧表示
- 差分表示
- 任意バージョンへの復元
- Agent 変更の一括リバート

### EX-002: スナップショット作成

**目的:**
テスト実行開始時点のテストケースを保持し、実行中にテストケースが編集されても実行結果に影響しない。

**スナップショット対象:**
- テストスイート情報
- テストスイート前提条件
- テストケース一覧
- 各テストケースの前提条件・手順・期待値

### レビューコメント（RC-001〜RC-005）

**コメント対象:**
- テストスイート
- テストケース

**コメント構造:**
```
ReviewComment
├── id
├── targetType (SUITE / CASE)
├── targetId
├── authorId (人 or Agent)
├── content
├── status (OPEN / RESOLVED)
├── createdAt
└── replies[]
    └── ReviewCommentReply
        ├── id
        ├── commentId
        ├── authorId
        ├── content
        └── createdAt
```

**ワークフロー:**
1. Agent がテストケースを作成・編集
2. 人がレビューコメントを登録
3. Agent がコメントを確認して修正
4. Agent がコメントに返信（対応内容を説明）
5. 人が確認してコメントを解決済みにする

### Agent 識別（AG-010, AG-011）

**識別方法:**
MCP リクエストに `X-MCP-Client-Id` ヘッダーを付与し、Agent 操作を識別する。

```
POST /mcp HTTP/1.1
Cookie: session=xxx
X-MCP-Client-Id: claude-desktop-v1.2.3
Content-Type: application/json
```

**Agent 操作の記録:**
- 履歴の `changedBy` に Agent 情報を記録
- レビューコメントの `authorType` で `USER` / `AGENT` を区別
- 編集ロックの `lockedByType` で `USER` / `AGENT` を区別

**セッション管理:**
- Agent は 30 秒間隔でハートビートを送信
- ハートビート途絶から 60 秒後にロックを自動解除
- 無操作 30 分でセッション終了

### 同時編集制御（CE-001〜CE-006）

**ロック対象:**
- テストスイート
- テストケース

**ロック方式:**

| 観点 | 人間ユーザー | Agent（MCP） |
|------|------------|----------------|
| 方式 | 悲観的ロック | 楽観的ロック |
| ロック取得 | 編集開始時に取得 | 取得しない（確認のみ） |
| ハートビート | 30 秒間隔で送信 | 不要 |
| 競合時 | 409 Conflict | 409 Conflict + 更新拒否 |

**ロック構造:**
```
EditLock
├── id
├── targetType (SUITE / CASE)
├── targetId
├── lockedByUserId (人間ユーザーのみ)
├── lockedAt
├── lastHeartbeat
└── expiresAt (90秒)
```

**人間ユーザーのロック:**
- 編集開始時にロック取得
- 30 秒間隔でハートビート送信
- ハートビート途絶 60 秒で自動解除

**Agent（MCP）の楽観的ロック:**
- ロックを取得せず、更新直前にロック状態を確認
- 人間がロック中の場合は更新を拒否
- 人間の編集を優先し、Agent は人間のロックを尊重

**強制ロック解除:**
- プロジェクト/組織の OWNER/ADMIN が強制解除可能
- 解除時に WebSocket でロック保持者に通知

---

## データベース設計への影響

この機能一覧から、以下のテーブルが必要：

### 新規追加が必要なテーブル

| テーブル | 用途 |
|---------|------|
| `ProjectEnvironment` | プロジェクトの環境設定（dev/stg/prod等のエンドポイント） |
| `TestSuitePrecondition` | テストスイートの前提条件（複数） |
| `TestCasePrecondition` | テストケースの前提条件（複数） |
| `TestCaseStep` | テストケースの手順（複数） |
| `TestCaseExpectedResult` | テストケースの期待値（複数） |
| `ProjectHistory` | プロジェクトの変更履歴 |
| `TestSuiteHistory` | テストスイートの変更履歴 |
| `TestCaseHistory` | テストケースの変更履歴 |
| `ReviewComment` | レビューコメント |
| `ReviewCommentReply` | レビューコメントへの返信 |
| `ExecutionSnapshot` | 実行時のスナップショット |
| `ExecutionStepResult` | 各手順の実施結果 |
| `ExecutionPreconditionResult` | 各前提条件の実施結果 |
| `ExecutionExpectedResult` | 各期待値の判定結果 |
| `EditLock` | 編集ロック管理 |

### 既存テーブルの変更

| テーブル | 変更内容 |
|---------|---------|
| `Project` | `ownerId` or `organizationId` の排他制約 |
| `TestCase` | `steps`, `expectedResult` カラムを削除（別テーブル化） |
| `Execution` | スナップショットへの参照追加、実行対象環境（`environmentId`）の参照追加 |

---

## 関連ドキュメント

- [データベース設計](../architecture/database.md)
- [API 設計方針](../architecture/api-design.md)
- [システム全体像](../architecture/overview.md)
